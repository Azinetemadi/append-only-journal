# append-only-journal
An append-only CLI journal that records timestamped entries to a local JSONL log and supports deterministic replay.
## There are 2 versions of this journal:
Journal-lite: the shortest version.
